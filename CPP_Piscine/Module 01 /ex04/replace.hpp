#ifndef REPLACE_H
#define REPLACE_H

#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <fstream>

#define ARG_ERR "ARG error!\nYou should turn in file name than string to be replaced and the string you want to replace to"
#define EMP_ERR "ARG error!\nOne of the strings is empty"

#endif